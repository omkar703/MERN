<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gray-900 text-white font-sans antialiased min-h-screen flex items-center justify-center p-6"
  >
    <section class="w-full max-w-md bg-gray-800 p-8 rounded-2xl shadow-lg">
      <h1 class="text-3xl font-bold text-center text-yellow-400 mb-6">
        Edit Profile
      </h1>
      <form action="/edit/<%= user._id %>" method="POST" class="space-y-5">
        <input
          type="text"
          name="name"
          value="<%= user.name %>"
          required
          class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-yellow-500"
        />
        <input
          type="email"
          name="email"
          value="<%= user.email %>"
          required
          class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-yellow-500"
        />
        <input
          type="url"
          name="image_url"
          value="<%= user.image_url %>"
          required
          class="w-full px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-yellow-500"
        />
        <button
          type="submit"
          class="w-full py-3 bg-yellow-500 hover:bg-yellow-600 transition rounded-lg font-semibold text-white"
        >
          Update Profile
        </button>
      </form>
    </section>
  </body>
</html>
