<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit - <%= fileName %></title>
    <link
      rel="icon"
      type="image/png"
      href="https://img.icons8.com/cute-clipart/64/task.png"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 text-white font-sans">
    <div
      class="min-h-screen flex flex-col items-center justify-start px-4 py-10"
    >
      <div
        class="w-full max-w-xl bg-zinc-800/90 backdrop-blur-md rounded-2xl p-6 shadow-[0_0_30px_rgba(0,0,0,0.4)]"
      >
        <h1 class="text-3xl font-semibold mb-6 text-center text-yellow-300">
          Edit Task
        </h1>
        <form action="/edit/<%= fileName %>" method="POST">
          <!-- Editable Title -->
          <input
            name="title"
            value="<%= fileName.replace('.txt', '') %>"
            class="block w-full rounded-xl bg-zinc-700 text-white placeholder-gray-400 px-5 py-3 mb-4 focus:ring-2 focus:ring-yellow-400 focus:outline-none"
            required
          />
          <!-- Editable Content -->
          <textarea
            name="description"
            rows="10"
            class="block w-full rounded-xl bg-zinc-700 text-white placeholder-gray-400 px-5 py-3 mb-4 focus:ring-2 focus:ring-yellow-400 focus:outline-none"
            required
          >
<%= content %></textarea
          >
          <!-- Submit -->
          <input
            type="submit"
            value="Update Task"
            class="w-full bg-gradient-to-r from-yellow-500 to-yellow-400 hover:from-yellow-400 hover:to-yellow-500 text-white font-medium py-3 rounded-xl shadow-md cursor-pointer transition"
          />
        </form>
        <div class="mt-4 text-center">
          <a href="/" class="text-gray-400 hover:underline text-sm"
            >&larr; Back</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
